pollutantmean <- function(directory, pollutant, id = 1:332) {
  path <- paste("./data/", directory, sep="")
  file_list <- list.files(path=path, pattern="*.csv")

  for (i in id) {
    data <- read.csv()
  }
}
